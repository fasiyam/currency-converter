import React from 'react';

const DropdownForm = ({name, id}) => {
    return (
        <div>
            <form>
                <select name={name} id={id}>
                    
                </select>
            </form>
        </div>
    );
};

export default DropdownForm;